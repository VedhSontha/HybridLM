# Contributing to LLM Pipelines

We welcome contributions to optimize models, refine tokenizers, or improve retriever queries.

## Guidelines
1. Fork the repo and create your branch from `main`.
2. Ensure Python virtual environment dependencies are isolated.
3. Add tests for new encoders or retrievers.
4. Keep models and large databases excluded via gitignore.
