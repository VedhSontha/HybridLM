# RAG Security: Input Sanitization

Defending against prompt injections in SRE workflows:
- Strips special control characters from incoming Loki log streams.
- Validates log inputs to restrict system prompt behavioral modification.
