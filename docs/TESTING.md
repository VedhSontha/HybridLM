# LLM Pipeline Testing Guidelines

Instructions for verifying retriever and LLM mock calls:
- Run tests: `pytest modules/`
- Validates semantic retrieval queries against FAISS databases and symbolic queries against SQLite FTS5.
