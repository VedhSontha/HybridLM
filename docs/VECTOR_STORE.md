# LLM Vector Store Schemas

FAISS index structure:
- Index Type: `IndexFlatL2` Euclidean distance index.
- Embeddings dimensions: 768d (BERT representation embeddings).
- FAISS vectors are saved locally to allow offline retrieval.
