# FAISS Euclidean Distance Metric ($L_2$)

Validating distance parameters in dense vector search:
- Metric: L2 (Euclidean) distance flat indexing (`IndexFlatL2`).
- Computes $d(x, y) = ||x - y||_2$ to rank similarity.
