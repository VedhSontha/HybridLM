# FAISS IndexFlatL2 Semantic Search

Evaluating document similarity using flat L2 index layers:
- Embeddings are compared using Euclidean distance.
- Index parameters: standard L2 index layer (`IndexFlatL2`).
- Provides fast, deterministic similarity scoring for local documents.
